Tutorial
========

In this tutorial, we will be making a simple API to retrieve and store
blog articles.  We will call the project ``blogapi``.

We assume that you already have Python 3 installed, which is required for FRF.
One you have followed the following steps, you should have a working blog api!

.. toctree::
   :maxdepth: 2
   :numbered:

   virtualenv
   installation
   firstmodule
   tryit
   authentication
   filters
   pagination
   renderers
